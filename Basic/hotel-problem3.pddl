(define (problem basic-hotel3)
   (:domain hotel)
   (:objects H1 - habitacion
             R1 R2 - reserva
    )

    (:init

        a modificar
    )

    (:goal (forall (?r - reserva) (not (libreR ?r))))
    
)

